<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dcservicez.a247services">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_new"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_new"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Profike_Managment"></activity>
        <activity android:name=".Reviw_User" />
        <activity android:name=".Chat_Conversations" />
        <activity android:name=".about_sp_profile" />
        <activity
            android:name=".SP_Main_Acitvity"
            android:label="@string/title_activity_sp__main__acitvity" />
        <activity android:name=".Customer_menu" />
        <activity android:name=".Sp_Profile" />
        <activity android:name=".Change_profile_pic" />
        <activity android:name=".Account_Managment" />
        <activity android:name=".Select_service" />
        <activity android:name=".Chat_activity" />
        <activity android:name=".Customer_Main" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".search_service">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH_SERVICE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".SignIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Signup_sp" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Sp_first_activity" />
    </application>

</manifest>